Django==4.2.7
Pillow>=10.3.0
geopy==2.4.1
python-decouple==3.8
psycopg2-binary==2.9.9
gunicorn==21.2.0
whitenoise==6.6.0

# Build dependencies for psycopg2
setuptools>=65.0.0

# Note: For easier deployment, consider using Railway or Render instead of Vercel
# Vercel is optimized for serverless/Next.js, not traditional Django apps

# Optional: Large ML libraries (commented out for Vercel deployment due to size limits)
# numpy>=1.24.0         # ~20MB - uncomment if advanced array processing needed
# opencv-python>=4.8.0  # ~100MB - uncomment if advanced image processing needed
# tensorflow>=2.16.0    # ~500MB - uncomment if ML model support needed
# To enable: uncomment above and update ai_recognition.py to use actual model


